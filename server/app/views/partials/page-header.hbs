<header class="cv-page-header">
  <h1>{{{title}}}</h1>
  <p>Tapez <code>ALT-L</code> (comme dans «&nbsp;Lien&nbsp;») pour afficher/masquer les liens brisés sur une page du site.</p>
</header>
<p><small>Liens vérifiés le <cv-job-list{{#if currentJob}} current-job="{{currentJob}}" {{/if}}>38 octembre 3017</cv-job-list>
      {{#if deadline }}<br />
      Échéance le {{ deadline }}{{/if}}</small>
</p>